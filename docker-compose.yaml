version: "3"
services:
  express-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bun-express-server
    ports:
      - "8080:8080"
    volumes:
      - ./dist:/usr/src/app/dist
      - ./:/usr/src/app
    environment:
      NODE_ENV: production
